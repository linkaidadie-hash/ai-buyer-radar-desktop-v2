# AI海外采购商雷达系统 V1
# 后端依赖

# Web框架
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# 数据库
# SQLite (内置，无需安装)

# HTTP客户端
httpx==0.27.0

# 数据处理（可选，用于Excel导出）
# openpyxl==3.1.2  # pip install openpyxl  # Windows可直接安装，无需编译

# AI API (通过httpx调用，无需额外SDK)